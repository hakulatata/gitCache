<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="LineView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0.5dp</item>
        <item name="android:background">@color/c8</item>
    </style>

    <style name="ios_bottom_dialog" parent="@android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!-- 弹窗动画 由下至上 -->
    <style name="ios_bottom_dialog_anim" parent="@android:style/Animation.Dialog">
        <item name="@android:windowEnterAnimation">@anim/ios_dialog_enter</item>
        <!-- 进入时的动画 -->
        <item name="@android:windowExitAnimation">@anim/ios_dialog_exit</item>
        <!-- 退出时的动画 -->
    </style>

    <style name="album_popupwindow_anim">

        <item name="android:windowEnterAnimation">@anim/slide_down</item>
        <item name="android:windowExitAnimation">@anim/slide_up</item>

    </style>

    <style name="gallery_popupwindow_anim">

        <item name="android:windowEnterAnimation">@anim/gallery_fade_in</item>
        <item name="android:windowExitAnimation">@anim/gallery_fade_out</item>

    </style>

   <!-- <style name="CustomRadioBtn">
        <item name="android:background">@color/white</item>
        <item name="android:button">@drawable/custom_radio_btn</item>
    </style>-->
</resources>
